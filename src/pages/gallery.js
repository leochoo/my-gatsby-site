import Frame from "../components/frame";
import * as React from "react";

const gallery = () => {
  return (
    <Frame>
      <p>This will be pass in as children</p>
    </Frame>
  );
};

export default gallery;
